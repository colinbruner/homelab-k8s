apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unas-scans-ro-claim
spec:
  accessModes:
    - ReadOnlyMany
  volumeName: unas-scans-ro
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi

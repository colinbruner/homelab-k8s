apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garage-data
  namespace: garage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi-buckets
  resources:
    requests:
      storage: 1Ti

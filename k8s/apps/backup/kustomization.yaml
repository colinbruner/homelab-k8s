apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: backup
helmGlobals:
  chartHome: ../../../packages/helm
helmCharts:
  - name: kopia
    releaseName: backup
    namespace: backup
    valuesFile: kopia-values.yaml
resources:
  - namespace.yaml
  - secrets.yaml

# 1Password item "garage-s3" in the lab vault.
# Required fields:
#   rpc_secret  — 64-character lowercase hex string (openssl rand -hex 32)
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: garage-secret
  namespace: garage
spec:
  itemPath: "vaults/lab/items/garage-s3"

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dagster-tls
  namespace: gateway
spec:
  secretName: dagster-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - dagster.colinbruner.com

apiVersion: v2
name: kopia-legacy
description: >
  Parameterized Kopia GCS backup deployment for the homelab cluster.
  Each target (documents, photos, etc.) provides its own values.yaml
  specifying the NFS source path, GCS bucket, schedule, and Chronos
  health-check configuration.
type: application
version: 0.1.0
appVersion: "0.23.1"

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | quote }}
spec:
  selector:
    app: postgres
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5432
  type: ClusterIP
